{
  "name": "presupuesto-app-web",
  "private": true,
  "description": "Versión estática (sin build) de presupuesto-app. package.json existe solo para el harness de pruebas (Playwright) -- la app en sí no tiene dependencias de build.",
  "scripts": {
    "test": "node tests/run-all.js"
  },
  "devDependencies": {
    "playwright": "1.56.1",
    "xlsx": "0.18.5",
    "pdfjs-dist": "3.11.174"
  },
  "dependencies": {
    "@vercel/functions": "^3.4.3"
  },
  "type": "module"
}
