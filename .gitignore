# Tu Excel real y cualquier copia de seguridad — nunca se suben al repo
*.xlsx
*.xls
!plantilla*.xlsx
# Fixtures sintéticos (datos inventados) para los tests de Playwright --
# ver tests/*.js.
!tests/*.xlsx
*.backup-*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# Node (harness de pruebas -- Playwright, xlsx, pdfjs-dist; ver package.json)
node_modules/

# App de escritorio (Electron) -- binarios compilados, ver desktop/package.json
desktop/dist/

# Sistema
.DS_Store
